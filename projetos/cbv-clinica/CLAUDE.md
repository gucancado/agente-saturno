@../../_base/CLAUDE.md
@PROJECT.md

# Overrides do projeto <slug>

Tudo acima é herdado: as regras compartilhadas do `_base` e o briefing em `PROJECT.md`.

Aqui ficam ajustes específicos deste projeto.

## Particularidades de operação

<Ex: "Cliente X prefere ser respondido em até 30min em horário comercial"; "Não usar emojis com cliente Y"; "Sempre confirmar agendamentos com 24h de antecedência".>

## Skills habilitadas neste projeto

Skills do `.claude/skills/` deste diretório são carregadas automaticamente. Skills do `_base/skills/` também (via symlink em `~/.claude/skills/_base`).

Skills específicas deste projeto:
- <listar quando houver>

## MCPs adicionais

Ver `.mcp.json` deste diretório.
