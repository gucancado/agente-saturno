# Hand-off saturno — 2026-06-14

Continuação do hand-off de 2026-06-13. Estado após implementar e **ativar a regra R1**
ponta-a-ponta + otimização de custo. Detalhes vivos na memória do projeto
(`saturno-estado-operacional`, `saturno-r1-deploy-2026-06-14`, `saturno-bug-traps-deploy`).

## O que foi entregue nesta sessão (tudo em prod, master nos 3 repos)

**Pipeline de identidade (A+B):**
- **bloquim-api**: PR #14 mergeado + deployado. Endpoint `GET /api/internal/resolve-by-whatsapp`
  (gate `X-Internal-Secret`). Smoke OK (200/404/401). CI estava vermelho por **versão do pnpm**
  (CI 9.15.9 vs deploy/lock 11.4.0), não lockfile — fix = bump do CI p/ 11.4.0 (1 linha).
- **worker** (`semente-platform-worker`, master): 2 tools MCP novas no platform —
  `resolve_whatsapp_identity` (telefone→user Bloquim) e `send_whatsapp_dm` (envio via número B).
  Smokes OK (objeto/null; `ok:true,send_id`).

**Regra R1 (C)** — `_base/skills/regra-promessa-vira-tarefa/SKILL.md` ativa:
- Promessa de **EQUIPE** (Camada 1 = membro do workspace via `resolve_whatsapp_identity`) no grupo
  → cria tarefa Bloquim (L0). Camada 2 (push_name) só levanta hipótese (anti-spoofing, nunca tarefa).
- Dedup por chave-slug `<digitos>:<objeto-slug>` (runtime do agente sem `head/tail/sha1sum`).
- `+ identidades.sh` (cache) + `ledger.sh` (dedup).

**Modo VEREDITO (teste) — LIGADO:** env `R1_VERDICT_DM_TO=5531999594121` no saturno → R1 manda
**DM com o veredito do tick** (via `send_whatsapp_dm`/número B) **em vez de** criar tarefa. Desligar
= remover o env + redeploy → volta a criar tarefa.

**Observabilidade do tick:** crontab usa `tee` + `log()` no stdout + dump do `CLAUDE_OUT` na falha
→ dá pra ver erro/resultado/custo do tick nos **logs do Coolify** (sem exec/SSH).

**Custo otimizado** (`scripts/cadencia.yml` perfil `sweep`):
- modelo **Haiku 4.5** (era sonnet-4-6; ~3-5x), `max_turns` **12** (era 40), caps tick **0.12**/dia
  **1.50** (eram 0.50/5.00), cadência **15min comercial / 45min off-hours+fds** (era */10),
  **slim de tools** via `--allowedTools` lendo `profiles.sweep.allowed_tools` (~49→16 tools;
  corta ~7-10k tok/turn). Degrada seguro (sem allowed_tools = sem restrição).

## Estado operacional
- Saturno **ATIVO** (`FORCE_NO_CHANGES=0`). Kill-switch: `=1` + redeploy.
- **Crédito Anthropic recarregado + testado OK** (key do saturno responde, sem "credit too low").
- **1º tick pago ainda NÃO confirmado end-to-end** — domingo 2026-06-14 a cadência é */45 →
  próximo tick **13:45 BRT** deve rodar e mandar o veredito (sobre as 2 msgs antigas de 2026-06-11
  no grupo → "nenhuma promessa"; cursor fresco pós-redeploy).
- Projeto auditado: **clubinho-da-historia** → workspace Bloquim `e21deb6f-ef7e-4cd5-a40f-fa503859d8a6`
  → grupo `+120363308683104573`. Owner gustavo (`5531999594121`) é admin do workspace.

## Pendências (próxima fase — ordem sugerida)
1. **Confirmar 1º tick pago**: ver logs Coolify do tick (`GET /applications/h5btft2bcfsz57mmmwf7do7q/logs`)
   → `claude OK ... resultado` + `total_cost_usd`; confirmar **veredito chegou no WhatsApp** do owner;
   **medir custo real** (valida o ganho Haiku+slim; conferir se `--allowedTools` reduziu schema).
2. **Fix de persistência (`.git`)**: `/workspace` não é repo git (`fatal: not a git repository`) →
   ledger/cursor/memória não persistem cross-redeploy. Bakear `.git` na imagem OU `git clone` no
   entrypoint OU volume persistente p/ `memoria/`. (Não bloqueia R1: `search_tasks` barra dup.)
3. **Flipar veredito→tarefa-real**: quando confiar no julgamento, remover `R1_VERDICT_DM_TO` + redeploy.
4. **Validar Haiku no "Amplo"**: ver se Haiku detecta bem coloquialismo ("pode deixar comigo"). Se
   fraco, considerar escalada multimodelo (agent-kernel) — só se métrica mostrar.
5. **Higiene**: 3 workspaces "Clubinho" duplicados no Bloquim (`e21deb6f`, `8b2a9419`, `5dc0e20a`).
6. **R8/R14** (digest/alertas via B) — dependem de envio-B (já existe agora) + template Cloud (fora 24h).
   ⚠️ Antes de regras **L2**: corrigir o hash da skill `aprovacao-humana` (usa `sha1sum`, negado no
   runtime do agente — quebrado em `--print`; R1 é L0, não afeta).

## Infra / como operar (sem SSH — chave rejeitada)
- **Coolify API** base `http://5.78.199.192:8000/api/v1`, token no CLAUDE.md global (rotaciona).
  uuids: saturno `h5btft2bcfsz57mmmwf7do7q`, worker `qlp2n4fi3jlklisftet1y7cz`, bloquim-api
  `vtam7v68bqpnqgn5abg367su`.
- Deploy: `POST /deploy?uuid=<app>` (deploy.yml dos repos está **quebrado** — secrets GH ausentes →
  push não auto-deploya; sempre manual).
- Envs (inclui secrets, campo `real_value`): `GET /applications/<uuid>/envs`. Update: `PATCH .../envs`.
- Logs: `GET /applications/<uuid>/logs?lines=N`.
- Spec/plano da R1: `docs/specs/2026-06-13-r1-promessa-vira-tarefa.md` + `docs/superpowers/plans/…`.
