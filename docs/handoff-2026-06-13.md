# Hand-off saturno — 2026-06-13

Estado pra continuar em novo chat. Detalhes vivos na memória do projeto
(`~/.claude/projects/c--Users-gusta-Projetos-agente-saturno/memory/`):
`saturno-estado-operacional.md` + `saturno-bug-traps-deploy.md`.

## O que é
Saturno = agente AUDITOR (sweep) dos projetos BeeAds. Lê grupos WhatsApp (nº A,
read-only, via worker) e só **fala via nº B** (Cloud API) a indivíduos. Roda em
container Claude Code (supercronic, tick a cada 10min), **freado** por
`FORCE_NO_CHANGES=1`.

## No ar / funcionando
- **Comandos `!` (pt-BR)** no worker (`src/commands/`), respondidos via B no `webhook-cloud`. `!oi`/`!status`/`!ajuda` testados live. Deploy = **worker master** (Coolify uuid `qlp2n4fi3jlklisftet1y7cz`).
- **Nº B** Cloud API conectado: phone_number_id `1151653484698272` (+5531 9085-8510), PIN 2-etapas `731904`. Mapeado em `WHATSAPP_CLOUD_NUMBERS_JSON` → saturno.
- **Saturno no menu** do agentes.beeads.com.br (kind=auditor; row em `workspace_agents` do Bloquim; deploy main).
- **Container saturno** (uuid `h5btft2bcfsz57mmmwf7do7q`) rodando, MCPs bloquim+platform (Evolution removido de propósito → não posta em grupo). Identidade Bloquim = owner gustavo (`a2cb18a8-a109-41b7-aab0-d967979c7481`).
- **Infra de sweep pronta** (atrás do freio): `scripts/lib/has-changes.sh` (gate por novidade + cursor `.sweep-cursor`), `scripts/lib/ledger.sh` (dedup JSONL via git).
- **Regras R1/R8/R14** rascunhadas em `_base/skills/regra-*` (inativas, ⚙️ CONFIGURAR).
- Grupo Clubinho mapeado: `120363308683104573@g.us` → workspace Bloquim `e21deb6f-ef7e-4cd5-a40f-fa503859d8a6`.

## Pendências (pra ativar saturno = tirar FORCE_NO_CHANGES)
1. **Mergear PR #14** `gucancado/beeads-bloquim` (endpoint interno `resolve-by-whatsapp`; `INTERNAL_API_SECRET` já no Coolify de bloquim-api+worker). Sem merge, identidade cai em fallback (pushName).
2. **Decisão: equipe BeeAds × cliente** (R1 só cria tarefa de promessa da EQUIPE). Recomendado: email `@beeads.com.br` via identidade. Confirmar.
3. **Verbos de "promessa"** (revisar rascunho R1).
4. **Owner do Clubinho** + preencher `projetos/clubinho-da-historia/PROJECT.md` (cliente/contatos) — p/ R8/R14.
5. **Envio-B no agente** (skill/tool chamando `/send-cloud`) + **template Cloud** "digest" — p/ R8/R14 (aviso proativo fora da janela 24h).
6. **1º tick supervisionado** só com R1: tirar freio + acompanhar logs via Coolify (SSH do servidor REJEITA a chave → sem exec; logs via `GET /api/v1/applications/<uuid>/logs`).

## Caminho recomendado (ordem)
R1 é a mais perto (só Bloquim L0). Sequência: merge PR #14 → decidir equipe×cliente (default @beeads.com.br) → fechar R1 ponta-a-ponta → tirar freio → tick supervisionado (cria tarefa de promessa real, zero msg externa) → depois envio-B + R8/R14 + multimodelo (`@beeads/agent-kernel`).

## Gotchas
- Coolify token rotaciona (~mensal) — ver global CLAUDE.md.
- worker repo local pode estar em `feat/lua-v1` (WIP local do user); Coolify deploya **master** — sempre `git push origin HEAD` com a branch certa / confira.
- mercurio `!zerar` foi revertido pelo user; mercurio não tem o padrão `!` ainda.
- Deploys NÃO são auto no push — disparar `POST /api/v1/deploy?uuid=<app>`.
