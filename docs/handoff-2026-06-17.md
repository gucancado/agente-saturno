# Hand-off saturno — 2026-06-17 (custo Fase 1 + multi-projeto em prod)

> Prompt auto-contido pra colar num chat novo limpo. cwd = `c:/Users/gusta/Projetos/agente-saturno`.
> Idioma: pt-BR. Escolhas recomendada-vs-alternativa: ir de recomendada sem perguntar.

## LEIA PRIMEIRO
- Este arquivo + memórias do projeto: `saturno-inbox-backlog`, `saturno-custo-tick`, `saturno-grupos-catalogo`, `saturno-estado-operacional`, `saturno-bug-traps-deploy`.
- Specs/planos desta sessão: `docs/superpowers/specs/2026-06-17-custo-saturno-escala-design.md` (+ `-baseline.md`), `docs/superpowers/plans/2026-06-17-custo-saturno-escala.md`, `docs/superpowers/plans/2026-06-17-rollout-prod-avaliacao.md`.

## O QUE É
Saturno = agente **auditor/sweep** da BeeAds (Haiku 4.5, container Coolify, supercronic por cron). A cada tick varre os projetos do `_platform/workspace-map.json`, e por projeto: gate barato não-LLM (`scripts/lib/has-changes.sh`) → se houver novidade+candidato, roda `claude --print` que executa a regra **R1** (`_base/skills/regra-promessa-vira-tarefa/SKILL.md`): promessa de EQUIPE no grupo vira tarefa Bloquim. **Modo veredito ON** (`R1_VERDICT_DM_TO=5531999594121`) → em vez de criar tarefa, escreve o **veredito no log** (fonte de verdade) + DM best-effort.

## ESTADO (tudo em `master`, branch local `feat/r1-verdict-mode`)
**Validado ao vivo nesta sessão:**
- R1 ponta-a-ponta (sweep-103): LID→tel (gustavo=EQUIPE), promessa detectada ("subo a campanha amanhã" → criaria tarefa), modo veredito respeitado.
- Pré-filtro de promessa no gate (sweep-107 promessa→roda; sweep-362 chitchat→skip $0).

**Implementado, deploy `zwdd6zdjrgccrmb6fl8bztaa` (commit `1bf5d2c`) — NÃO validado ao vivo ainda:**
- **4 projetos monitorados** (workspace-map): `clubinho-da-historia`, `cbv-clinica`, `tagless-brasil`, `bluma-cf` (todos modo veredito). JIDs/workspaces na memória `saturno-grupos-catalogo`.
- Fix do **JID legado** (hífen): `has-changes.sh` + skill R1 trocaram `tr -cd '0-9'` por `sed 's/@.*//'` (preserva hífen). Sem isso Tagless (JID `553195857308-1578927607@g.us`) nunca casaria.
- **Validação pendente:** o 1º tick pós-deploy ainda não rodou quando este doc foi escrito. Conferir nos logs que os 4 projetos aparecem no sweep e que CBV (tem msg "envio para validação" = candidato) ATIVA e produz veredito. Rodar `scripts/ops/collect-verdicts.ps1` (`$env:COOLIFY_TOKEN=...`).

**Custo (Fase 1 do plano):** baseline tick ativo ~$0,14 (37% cache_creation + 33% cache_read + 30% output). Levers entregues: pré-filtro (corta ticks de chitchat) + caps por-projeto (`cost_cap_usd_per_project_day` $0,60, global $12/dia, tick $0,30). Deferido: `--disallowedTools` (Task 5, ~10%, mede-antes).

## O QUE ESTÁ FALTANDO (ordem de prioridade)
1. **🔴 LID→telefone dos membros REAIS** (gargalo da avaliação em prod): `_platform/lid-map.json` só tem o LID de teste do gustavo. Membros reais dos 4 grupos chegam como LID não-mapeado → R1 classifica "cliente" → **promessa de equipe NÃO detectada**. Sem resolver, os vereditos em prod virão vazios. Dois caminhos: (a) **worker extrai telefone do payload Evolution** (`participantPn`/`participantAlt` em `src/webhook/evolution.ts`) — estrutural, mata o lid-map; (b) mapear LIDs na mão conforme aparecem nos vereditos. **Recomendado: (a).**
2. **Validar o sweep de 4 projetos ao vivo** (poller pode já ter rodado — checar logs/`collect-verdicts.ps1`).
3. **Worker (outro chat, coordenar — `NOTA-saturno-chat-2026-06-16.md` no repo do worker):** C1 telefone do payload (item #1a) + C2 `identifier` no `/inbox-debug` (fecha parede FIFO do gate; hoje `has-changes` puxa 200 global e filtra client-side). Registrar Bluma CF no catálogo do worker (subject+project) — não estava lá.
4. **Task 5 `--disallowedTools`** (custo ~10%): plano Tasks 4-5; medir trim antes; NÃO remover `--allowedTools` às cegas (gateia execução headless).
5. **Go-live:** quando a avaliação convencer → limpar env `R1_VERDICT_DM_TO` + redeploy → R1 cria tarefas reais no Bloquim.

## INFRA / COMO OPERAR (sem SSH — chave rejeitada)
- **Coolify API** base `http://5.78.199.192:8000/api/v1`, token no `~/.claude/CLAUDE.md` global. uuids: saturno `h5btft2bcfsz57mmmwf7do7q`, worker `qlp2n4fi3jlklisftet1y7cz`, bloquim-api `vtam7v68bqpnqgn5abg367su`. Deploy: `POST /deploy?uuid=`. Logs: `GET /applications/<uuid>/logs?lines=N` — **parsear com PowerShell `ConvertFrom-Json`** (parse Python quebra). Envs: `GET /applications/<uuid>/envs` (`real_value`).
- **Worker MCP/REST via curl:** `X-Agent-Token` = `WORKER_TOKEN` do env do saturno (`6cb03ab0…`). `POST /mcp` (JSON-RPC, resposta SSE, msgs stringificadas em `result.content[0].text`). `GET /inbox-debug?limit=200` (max 200). **Catálogo de grupos:** `GET /admin/agents/saturno/groups` com `X-Owner-Token` = `OWNER_ADMIN_TOKEN` do env do worker (`f23d7cca…`) → jid+subject+project.
- **jq NÃO está no Git Bash local**; usar PowerShell. `c:/tmp` p/ arquivos temp (Git Bash `/tmp` ≠ Windows).
- **Modo veredito:** env `R1_VERDICT_DM_TO=5531999594121`. **Kill-switch:** `FORCE_NO_CHANGES=1` + redeploy.
- **Coletor de vereditos:** `scripts/ops/collect-verdicts.ps1` (`$env:COOLIFY_TOKEN=...; ./scripts/ops/collect-verdicts.ps1`).
- **Bug-traps:** 1º tick pós-deploy pode cold-start falhar (retry de 30s cobre); `running:unknown` no Coolify é quirk (container OK se ticks firam); `.git` não persiste no /workspace (cursor reseta no redeploy — git-ops gateados, sem erro); DM via número B só entrega na janela 24h (log é a fonte).

## PRÓXIMO PASSO CONCRETO
1. Conferir o poller/logs: 4 projetos no sweep + CBV ativou? (`collect-verdicts.ps1`).
2. Decidir o gargalo do LID (item #1): priorizar C1 no chat do worker (recomendado) OU começar a mapear LIDs na mão.
3. Seguir a observação (plano rollout) até bater critério → go-live.
